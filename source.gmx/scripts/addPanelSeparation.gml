///addPanelSeparation(text, sprite, tooltip)
var map = ds_map_create();
map[? "Text"] = argument[0];
map[? "Icon"] = argument[1];
map[? "Tooltip"] = argument[2];
map[? "Active"] = false;
map[? "Callback"] = undefined;

return map;
